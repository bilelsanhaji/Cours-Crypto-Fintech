# Slides — version étudiante

Ces fichiers sont **générés automatiquement** depuis les sources par
`_build-version-etudiante.py`, à la racine du dépôt.

Pour compiler :

```bash
cd 01-Slides/etudiant
quarto render
```

Les raccourcis de présentation sont décrits dans `../README.md`.
